<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE project>
<project name="FancyLayouts Tomcat Deploy" basedir="." default="all">

    <property name="deploy.war" value="${basedir}/target/FancyLayoutsDemo.war" />
    <property name="tomcat.classpath" value="/usr/share/tomcat7/lib/" />

	<property file="${user.home}/.ant/fancylayouts-tomcat.properties" />

	<taskdef resource="org/apache/catalina/ant/catalina.tasks"
		classpathref="${tomcat.classpath}" />

	<target name="tomcat.undeploy">
		<undeploy url="${deploy.url}" failOnError="false" username="${deploy.user}"
			password="${deploy.pass}" path="${deploy.context}" />
	</target>

	<target name="tomcat.deploy">
		<deploy url="${deploy.url}" username="${deploy.user}" password="${deploy.pass}"
			path="${deploy.context}" war="file:${deploy.war}" />
	</target>

	<target name="tomcat.start">
		<start url="${deploy.url}" failOnError="false" username="${deploy.user}"
			password="${deploy.pass}" path="${deploy.context}" />
	</target>

	<target name="tomcat.stop">
		<stop url="${deploy.url}" failOnError="false" username="${deploy.user}"
			password="${deploy.pass}" path="${deploy.context}" />
	</target>
</project>
